class Pattern {
    /**
     * @param {string} regex
     * @param {string} command
     * @param {string} rest
     */
    constructor(regex, command, rest) {
        this.regex = regex;
        this.command = command;
        this.rest = rest;
    }
}